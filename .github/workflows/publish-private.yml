name: Publish to Private Registry

on:
  push:
    tags:
      # Match stable release tags (e.g. v2026.3.7) and fork patches (e.g. v2026.3.8-1)
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-[0-9]+"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish (e.g. v2026.3.7)"
        required: true
        type: string

concurrency:
  group: publish-private-${{ github.event.inputs.tag || github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      PUBLISH_REF: ${{ github.event.inputs.tag || github.ref }}
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v4
        with:
          ref: ${{ env.PUBLISH_REF }}
          submodules: false

      - name: Fetch workflow files from main
        run: |
          git fetch origin main --depth=1
          git checkout origin/main -- .github/

      - name: Setup Node environment
        uses: ./.github/actions/setup-node-env
        with:
          install-bun: "false"
          use-sticky-disk: "false"

      - name: Build
        run: pnpm build && pnpm ui:build

      - name: Configure private registry auth
        run: |
          echo "//npmlib.amybot.ai/:_authToken=${PRIVATE_NPM_TOKEN}" > .npmrc
        env:
          PRIVATE_NPM_TOKEN: ${{ secrets.PRIVATE_NPM_TOKEN }}

      - name: Rebrand package for private registry
        run: |
          VERSION="${PUBLISH_REF#refs/tags/}"
          export VERSION="${VERSION#v}"
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.name = '@amybot/openclaw';
            pkg.version = process.env.VERSION;
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "
          echo "Rebranded as @amybot/openclaw@${VERSION}"
          # Patch CORE_PACKAGE_NAMES in dist to recognize the rebranded name
          find dist -name '*.js' -exec grep -l 'new Set(\["openclaw"\])' {} + \
            | xargs -r sed -i 's/new Set(\["openclaw"\])/new Set(["openclaw", "@amybot\/openclaw"])/g'
          echo "Patched CORE_PACKAGE_NAMES in dist files"

      - name: Publish to private registry
        run: |
          set -euo pipefail
          VERSION="${PUBLISH_REF#refs/tags/}"
          VERSION="${VERSION#v}"
          npm publish --registry https://npmlib.amybot.ai/ --access restricted --no-git-checks --ignore-scripts

      - name: Verify
        run: |
          VERSION="${PUBLISH_REF#refs/tags/}"
          VERSION="${VERSION#v}"
          echo "Published @amybot/openclaw@${VERSION} to https://npmlib.amybot.ai/"
